apiVersion: v1
kind: Secret
metadata:
  name: paless-secret
  labels:
    app: paless
type: Opaque
stringData:
  # IMPORTANT: Copy this file to paless-secret.yaml and replace with actual values
  # DO NOT commit paless-secret.yaml to version control
  db-password: "your-secure-database-password-here"
  minio-root-user: "admin"
  minio-root-password: "your-secure-minio-password-here"

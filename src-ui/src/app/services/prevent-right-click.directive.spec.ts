import { PreventRightClickDirective } from './prevent-right-click.directive';

describe('PreventRightClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventRightClickDirective();
    expect(directive).toBeTruthy();
  });
});

# ============================================================================
# Paless Central Configuration File
# ============================================================================
# This file contains all configurable values for the Paless deployment.
# Copy this file to a secure location and customize values for your environment.
#
# Development: These defaults are suitable for local development
# Production: Review and change ALL values marked with "# CHANGE IN PRODUCTION"
# ============================================================================

# ----------------------------------------------------------------------------
# Namespace Configuration
# ----------------------------------------------------------------------------
# Kubernetes namespace where all Paless resources will be deployed
PALESS_NAMESPACE=paless

# ----------------------------------------------------------------------------
# Container Registry Configuration
# ----------------------------------------------------------------------------
# Container registry for storing and pulling images
# Development: localhost:5001 for local K3s registry
# Production: Use your production registry (e.g., ghcr.io, docker.io)
REGISTRY=localhost:5001

# ----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ----------------------------------------------------------------------------
# PostgreSQL is used as the primary database for Paperless-ngx

# Database name for Paperless application
POSTGRES_DB=paperless

# Database user for Paperless application
POSTGRES_USER=paperless

# Database password
# CHANGE IN PRODUCTION - Use a strong, randomly generated password
POSTGRES_PASSWORD=dev-postgres-password-changeme

# ----------------------------------------------------------------------------
# MinIO Object Storage Configuration
# ----------------------------------------------------------------------------
# MinIO provides S3-compatible object storage for documents and media

# MinIO root/admin username
# CHANGE IN PRODUCTION - Use a secure admin username
MINIO_ROOT_USER=admin

# MinIO root/admin password
# CHANGE IN PRODUCTION - Use a strong, randomly generated password (min 8 chars)
MINIO_ROOT_PASSWORD=dev-minio-password-changeme

# Default bucket name for document storage
MINIO_BUCKET=paperless-documents

# ----------------------------------------------------------------------------
# Paperless-ngx Application Configuration
# ----------------------------------------------------------------------------

# Django secret key used for cryptographic signing
# CHANGE IN PRODUCTION - Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
PAPERLESS_SECRET_KEY=dev-secret-key-change-in-production-x7k9m2p5q8w1r4t6

# Timezone for the application (use TZ database format)
# Examples: America/New_York, Europe/London, Asia/Tokyo, UTC
PAPERLESS_TIME_ZONE=UTC

# OCR language for document processing
# Common values: eng (English), deu (German), fra (French), spa (Spanish)
# Multiple languages: eng+deu+fra
# See Tesseract language codes: https://tesseract-ocr.github.io/tessdoc/Data-Files-in-different-versions.html
PAPERLESS_OCR_LANGUAGE=eng

# ----------------------------------------------------------------------------
# Additional Notes
# ----------------------------------------------------------------------------
#
# Security Checklist for Production:
# - [ ] Change POSTGRES_PASSWORD to a strong password
# - [ ] Change MINIO_ROOT_USER and MINIO_ROOT_PASSWORD
# - [ ] Generate new PAPERLESS_SECRET_KEY
# - [ ] Update REGISTRY to your production registry
# - [ ] Set appropriate PAPERLESS_TIME_ZONE for your location
# - [ ] Configure PAPERLESS_OCR_LANGUAGE for your documents
# - [ ] Store this file securely (use secrets management)
# - [ ] Do not commit production secrets to version control
#
# Environment-specific overrides:
# - Development: Use these defaults
# - QA/Staging: Override sensitive values in k8s/overlays/qa/
# - Production: Override ALL sensitive values in k8s/overlays/prod/
#
# ============================================================================
